# Mobile app

Bootstrap target: Expo SDK 56 + React Native 0.85, Expo Router and TypeScript strict mode.

Codex must create the project in Phase P2 using the UX Bible, generated API client and design tokens. Do not add cloud document upload to the initial implementation. Use a repository layer so local SQLite/secure storage can be replaced without changing screens.
