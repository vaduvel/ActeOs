# apps/curator-web

Portal curator (Next.js + React, TypeScript strict). Flux: fetch → diff → draft → review → publish → rollback.

Status: schelet P0 — neimplementat. Vezi P5 în `16_PHASE_PROMPTS.md`.
