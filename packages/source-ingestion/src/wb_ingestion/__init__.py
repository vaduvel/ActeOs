"""wb_ingestion — source ingestion and content lifecycle.

Safe source registry, bounded fetcher, immutable snapshots,
normalization, diff severity, impact analysis, optional AI draft
extraction, human review, atomic publishing, rollback and staleness.
"""

__version__ = "0.1.0"
