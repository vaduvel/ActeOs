version: 1
flags:
  citizen_account:
    default: false
    description: Optional account/sync; anonymous device flow remains available.
  document_metadata_sync:
    default: false
    description: Explicit opt-in only; never sends image bytes or raw OCR.
  ai_candidate_extraction:
    default: false
    description: Curator ingestion helper; cannot publish.
  high_school_journey:
    default: false
    activation_gate: verified_county_booklet_and_codes
  roeid_auth:
    default: false
    activation_gate: formal_agreement_sandbox_security_review
  anaf_oauth:
    default: false
    activation_gate: scoped_service_enrolment_and_contract_tests
  ghiseul_integration:
    default: false
    activation_gate: formal_integration_approval
  official_deep_links:
    default: true
    activation_gate: allowlisted_source_registry
  push_rule_change_notifications:
    default: false
    activation_gate: consent_and_provider_configuration
